# Kkubuck 연구 노트 — 디자인 시스템과 구현 기준

이 문서는 현재 저장소의 시각·상호작용 원칙을 다시 구현하거나 유지보수할 때 사용하는 기준입니다. 특정 사이트의 화면을 복제하지 않고, 여러 공개 디자인 시스템에서 검증된 원칙을 연구 블로그의 읽기 흐름에 맞게 조합했습니다.

## 1. 방향

핵심 비유는 **연구자 책상 위의 필드 노트**입니다. 화면 효과보다 글이 먼저 보여야 하며, 논문 제목·핵심 요약·본문·출처·비평이 자연스러운 순서로 이어져야 합니다.

- 배경은 따뜻한 종이색, 본문은 짙은 잉크색을 기본으로 합니다.
- 블루는 링크와 현재 상태, 코랄은 작은 신호와 진행 표시에서만 사용합니다.
- 유리판·네온·과도한 그라디언트·대문자 영문 라벨의 반복을 피합니다.
- 카드마다 같은 그림자를 강요하지 않고, 선·여백·타이포그래피로 계층을 만듭니다.
- 장식 인터랙션은 콘텐츠를 가리지 않으며 포인터 입력과 모션 감소 설정을 존중합니다.

## 2. 참고한 공개 시스템

- GitHub Primer — 색·간격·타이포그래피 토큰, 접근성 중심의 컴포넌트 설계
  https://primer.style/
  https://primer.style/foundations/typography
  https://primer.style/accessibility/tools-and-resources/checklists/engineering-checklist/
- Vercel Geist — 작은 UI에서의 명확한 위계, 일관된 글자 크기·행간 조합, 고대비 색 체계
  https://vercel.com/geist/introduction
  https://vercel.com/geist/typography
- Linear — 정보 밀도가 높은 화면에서의 절제된 모션과 빠른 탐색 패턴
  https://linear.app/
- GitHub Pages 공식 Jekyll workflow — 저장소에서 바로 빌드·배포하는 구조
  https://github.com/actions/starter-workflows/blob/main/pages/jekyll.yml

## 3. 타이포그래피

- 기본 크기: `clamp(1.04rem, 1rem + 0.18vw, 1.15rem)`
- 논문 본문 폭: 최대 `760px`
- 본문 행간: `1.84`, 목록: `1.7` 전후
- 큰 제목은 화면 폭에 따라 줄어들고 모바일에서 본문을 압도하지 않아야 합니다.
- UI 크기는 `rem`, 행간은 단위 없는 값으로 설정해 브라우저 확대에 대응합니다.
- 제목은 `text-wrap: balance`, 문단은 가능한 환경에서 `text-wrap: pretty`를 사용합니다.

## 4. 화면 구조

- **홈**: 소개 → 최신 리뷰 → 최근 논문 → 연구 기록 → 프로필
- **논문**: 검색 → 학회/연도/태그 필터 → 제목·요약 중심 목록
- **본문**: 메타데이터 → 핵심 요약 → 본문 → 정리 범위 안내 → 이전/다음 글
- **데스크톱 본문**: 왼쪽 고정 목차 + 중앙 읽기 영역
- **모바일 본문**: 목차를 숨기고 읽기 영역을 전체 폭으로 확장
- **CV**: 프로필과 연락처는 보조 열, 논문·경력·수상은 주 열

## 5. 상호작용

- `⌘/Ctrl + K`: 전체 검색
- `/`: 아카이브 검색창 포커스
- 스크롤: 본문 진행률과 현재 목차 항목 갱신
- 포인터: 홈 주제 지도의 2–7px 이내 미세 이동, 배경 물결 생성
- 캔버스: 저밀도 빗방울과 짧은 수면 파동만 표시
- `prefers-reduced-motion: reduce`: 장식 애니메이션 제거
- 탭 이동, 포커스 트랩, `aria-*`, 320px 최소 폭을 기본 검증 항목으로 둡니다.

## 6. 논문 글 편집 기준

1. 논문이 해결하려는 실패 조건을 먼저 설명합니다.
2. 모듈 이름보다 각 설계가 필요한 이유와 정보 흐름을 연결합니다.
3. 실험 수치는 원문에서 확인되는 경우에만 쓰고, 설정이 다른 숫자를 직접 비교하지 않습니다.
4. 평균 성능 외에 데이터 분할, 추가 사전학습, 입력 해상도, 계산량과 실패 사례를 함께 봅니다.
5. 직접 재현한 결과와 원 논문이 보고한 결과를 명확히 구분합니다.
6. 글마다 문제·방법·검증·한계가 실제 논문에 맞게 달라야 하며 동일한 문장 틀을 반복하지 않습니다.

## 7. 배포 전 확인

```bash
bash _scripts/verify.sh
```

추가로 1440×900, 768×1024, 390×844 화면에서 홈·논문 목록·본문·검색 팔레트·모바일 메뉴를 확인합니다. 텍스트가 잘리거나 가로 스크롤이 생기지 않아야 하며, 다크 모드와 모션 감소 환경에서도 핵심 기능이 유지되어야 합니다.
